---
import './Box.css'

type Props = {
    padding?: string //"var(--s1)"
    borderWidth?: string //"var(--border-thin)"
    invert?: boolean // false
}
const props = Astro.props
---

<box-l {...props}>
    <slot />
</box-l>

<script src="./Box.ts"></script>
